# Update feed tree

HTTPS template used by production clients:

`https://raw.githubusercontent.com/RedeatI/useful/main/deploy/update-feed/{channel}/{platform}/{arch}/latest.json`

Current files are empty placeholders (`status: empty`). Do not treat them as a live update channel until signed manifests are published and the signed Release workflow is green.
