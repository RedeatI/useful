# Useful v0.1.0-beta.10 Agent Kit post-publish acceptance

Date: 2026-08-14
Release: [`v0.1.0-beta.10`](https://github.com/RedeatI/useful/releases/tag/v0.1.0-beta.10)
Asset: `Useful-0.1.0-beta.10-agent-kit.zip`
Asset SHA-256: `705b23242b666e5c99a943dcb561e262a1bfd469c16aa1cc5eb88fb18a01498b`
Source revision: `737d9bc86be3d97f9e5dc12511eaab48476a20e0`

This is a post-publish execution record for the exact public asset. It does not authorize another
Release and does not upgrade the unsigned desktop preview to a signed or production channel.

## Environment and archive

- Windows x64, PowerShell, Node.js `24.14.0` (satisfies the documented `>=20` requirement).
- The downloaded ZIP was 3,556,193 bytes and matched its release `.sha256` sidecar.
- ZIP preflight found 112 entries, no rooted/traversal/colon paths, and no case-insensitive duplicate
  paths.
- `MANIFEST.json` identified Useful `0.1.0-beta.10` at source revision `737d9bc...`.
- All 111 manifest closure entries matched the extracted path, size, and SHA-256 exactly. The
  self-excluded `MANIFEST.json` was present once.

## Non-interactive author flow

The Windows launchers from the extracted Kit were used with absolute paths. Every target command
returned exit code 0, exactly one JSON document, and empty stderr.

| Step | Result |
| --- | --- |
| `agent-contract --json` | `useful.cli.result.v1`, `ok:true` |
| default `actions list --json` | `actions.list`, 36 built-in Actions |
| `create ... --template minimal-action --json` | zero-permission declarative plugin created |
| `doctor ... --json` | 13/13 checks passed; 0 warnings |
| `validate ... --json` | 6/6 checks passed; `valid:true` |
| `pack ... --json` | 1,910 bytes, 4 entries, SHA-256 `832667d65f7a8b4e2bbeac55cf505402d3aaf93dec6ce790a3e654ae1136d08e` |
| publisher init/sign/verify | test-only Ed25519 publisher; `valid:true`; artifact hash matched independently |

The acceptance publisher was generated only for this isolated test. Its private key, local paths,
raw evidence files, and generated artifacts were not committed or uploaded.

## Runtime and MCP

An explicit `useful.plugin-set.v1` used the fresh publisher-key and artifact-SHA dual pins.

- Runtime list expanded from 36 to 37 Actions and exposed the plugin exactly once.
- Runtime describe derived `source.kind=plugin`, the pinned publisher, and a 64-hex source digest.
- Runtime run of `com.redeati.acceptance-tool.base64-sha256` with `{"text":"abc"}` returned the
  expected digest `35d95694d3f160215db293c7899daa5907837838fb4b8119ed713e32446c1266`.
- The official MCP client exercised the server entry from the published Kit in both protocol eras:

| Protocol era | Negotiated version | Tool count | Plugin count | Call result |
| --- | --- | ---: | ---: | --- |
| legacy | `2025-11-25` | 41 | 1 | expected digest |
| modern | `2026-07-28` | 41 | 1 | expected digest |

Both MCP sessions closed with no remaining child process and empty stderr. The MCP client was the
acceptance driver supplied by the checkout; the server, runtime, CLI, schemas, workers, and plugin
under test all came from the downloaded Release asset.

## Cross-platform Node.js 20 acceptance

[`Platform Bundles` run `31781531217`](https://github.com/RedeatI/useful/actions/runs/31781531217)
downloaded the same immutable Release asset and passed all seven jobs at acceptance-driver revision
`883097b8f2d568890f9a0d4706c3b5e42a793807`. The three Agent Kit jobs used Node.js `v20.20.2`:

| Runner | Launcher exercised | Closure | Runtime / workers | MCP |
| --- | --- | ---: | --- | --- |
| Windows x64 | Windows `.cmd` | 111/111 | 36 Actions; suggest, recipe, regex and Office passed | legacy `2025-11-25` + direct-entry modern `2026-07-28`; 40 tools |
| macOS arm64 | POSIX | 111/111 | 36 Actions; suggest, recipe, regex and Office passed | legacy `2025-11-25` + direct-entry modern `2026-07-28`; 40 tools |
| Linux x64 | POSIX | 111/111 | 36 Actions; suggest, recipe, regex and Office passed | legacy `2025-11-25` + direct-entry modern `2026-07-28`; 40 tools |

Every receipt independently matched asset SHA-256
`705b23242b666e5c99a943dcb561e262a1bfd469c16aa1cc5eb88fb18a01498b`, source revision
`737d9bc86be3d97f9e5dc12511eaab48476a20e0`, the 111-entry MANIFEST closure, and the expected
SHA-256 call result. The repository supplied the acceptance driver and official MCP client only;
the executed CLI, runtime, server, launchers, regex worker and Office worker came from the Release
ZIP. Aggregate-only receipts were uploaded as run artifacts for 14 days.

## Evidence boundary

- Verified: the exact published Windows and POSIX launcher paths on Windows x64, macOS arm64 and
  Linux x64; Node.js 20; archive closure; author/signing sequence; signed plugin runtime
  registration/execution; worker-bundle calls; and legacy/modern MCP list/call behavior.
- Not executed: third-party Agent-host UI integration, the macOS x64 Agent Kit launcher, or other
  architecture-specific launcher combinations.
- Not claimed: production signing, Authenticode, notarization, production update trust, public
  contribution intake, or publication of the generated test plugin.
